services:
  - type: web
    name: stock-control-app
    env: java
    plan: free
    buildCommand: apt-get update && apt-get install -y maven && mvn clean package -DskipTests
    startCommand: java -jar target/stock-control-app-1.0.0.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
